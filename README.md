# 遠隔カンペ (kanpe.html)
 
登壇者の足元に置いたPC・タブレットに、タイムキーパー役がリアルタイムでメッセージや残り時間を送れる「遠隔カンペ」ツールです。単一のHTMLファイルで動作し、Firebase Realtime Databaseを使って2台の端末間を同期します。
 
## できること
 
- **表示用画面**：登壇者のスピーカー足元に置くPC用。送られてきたメッセージを画面いっぱいに大きく表示します。
- **操作用画面**：タイムキーパー用。定型ボタン（GO / OK / 巻いて / 残り3分 など）や自由入力メッセージをワンタップで送信できます。
- **緊急表示**：赤背景で点滅する「緊急」送信も可能。
- **接続状態の可視化**：お互いの接続確認を行い、「接続中 / 応答なし / ネット接続なし」をリアルタイム表示。
- URLパラメータで自動起動可能（後述）。
## 必要なもの
 
- Firebaseプロジェクト（無料のSparkプランで十分）
- Realtime Database（Firestoreではなく **Realtime Database**）
- Wi-Fi等、両端末が同じインターネットに出られる環境
## セットアップ手順
 
### 1. Firebaseプロジェクトを作成
 
1. [Firebase コンソール](https://console.firebase.google.com/) にアクセスし、新規プロジェクトを作成。
2. 左メニューの「Realtime Database」を開き、「データベースを作成」。
3. リージョンを選択（例：`asia-southeast1` など近い場所）。
4. セキュリティルールは、ひとまず「テストモード」で開始してOK（後述の「セキュリティに関する注意」も参照）。
### 2. `databaseURL` を書き換える
 
プロジェクト作成後に発行される Realtime Database の URL を、HTML内の以下の箇所に貼り付けます。
 
```javascript
const firebaseConfig = {
  databaseURL: "https://あなたのプロジェクトID-default-rtdb.リージョン.firebasedatabase.app"
};
```
 
このアプリは `databaseURL` だけで動くように作られています（`apiKey`などの他の設定値は不要です）。
 
### 3. Realtime Databaseのルールを設定
 
Firebaseコンソール → Realtime Database → 「ルール」タブで、最低限アプリが動く形にします。例：
 
```json
{
  "rules": {
    "rooms": {
      "$room": {
        ".read": true,
        ".write": true
      }
    }
  }
}
```
 
> ⚠️ 上記は「ルーム名さえ知っていれば誰でも読み書きできる」設定です。イベント当日だけ使う想定であれば実用上は問題になりにくいですが、詳しくは末尾の「セキュリティに関する注意」を参照してください。
 
### 4. ファイルを配置する
 
`kanpe.html` を、GitHub Pages・Netlify・社内サーバーなど、2台の端末からアクセスできる場所にアップロードします（ローカルの `file://` でも動作しますが、URLパラメータでの自動起動やスマホからのアクセスを考えるとWeb公開がおすすめです）。
 
## 使い方
 
### 起動時のモード選択
 
`kanpe.html` を開くと、最初に「表示用」「操作用」を選ぶ画面が出ます。
 
- **表示用** → スピーカー足元のPCで選択。ルーム名を入力（両端末で同じ名前にする）。
- **操作用** → タイムキーパーのPCで選択。ルーム名を入力して「接続」。
### URLパラメータで自動起動（本番運用向け）
 
毎回モードやルーム名を入力する手間を省くため、URLに直接パラメータを付けられます。
 
```
kanpe.html?mode=display&room=TEDX2026   ← 表示用（自動でこのルームに接続）
kanpe.html?mode=ctrl&room=TEDX2026      ← 操作用（自動でこのルームに接続）
```
 
- `room` は英数字のみ（自動で大文字化・記号除去されます）。
- 本番当日は、表示用PCのブラウザをこのURLでブックマークしておくとスムーズです。
### 操作用画面のボタン
 
| セクション | ボタン例 | 用途 |
|---|---|---|
| 進行指示 | GO / OK / 次行って / 休憩にしてOK / 巻いて / 少し早口で / のばして / クリア | 定型の進行合図 |
| 時間 | 10分経過 / 15分経過 / 残り5分・3分・2分・1分 | 経過・残り時間の通知 |
| 自由入力 | テキスト欄＋送信／緊急(点滅)で送信 | 上記にない任意のメッセージ。Enterキーで通常送信 |
 
送信した内容は操作用画面の「現在の表示」欄にも表示され、今何を表示中か確認できます。
 
### 表示用画面の操作
 
- 右上の「全画面」ボタンでフルスクリーン表示に切り替え可能。
- 左上のバッジで接続状態を確認：
  - 緑「操作側と接続中」＝正常
  - 赤「操作側の応答なし」＝タイムキーパー側がオフラインの可能性
  - 赤「ネット接続なし」＝表示側自体がネットに繋がっていない
## 表示ロジックについて
 
- 定型ボタンのメッセージは折り返さず1行で画面幅いっぱいに自動フィットします。
- 自由入力メッセージは文字数に応じて1〜4行に自動で折り返し、収まるようにフォントサイズが調整されます。
- 空文字（クリア）を送ると画面は元の暗い背景に戻ります。
## トラブルシューティング
 
| 症状 | 確認ポイント |
|---|---|
| 最初に赤いエラー画面が出る | `firebaseConfig` の `databaseURL` が空になっていないか確認 |
| 「操作側の応答なし」から変わらない | 表示用・操作用で**ルーム名が完全一致**しているか確認（大文字小文字は自動変換されます） |
| 送信しても表示側に反映されない | Realtime Databaseの「ルール」タブで read/write が許可されているか確認 |
| 数分後に急に繋がらなくなる | 片方の端末がスリープ/画面ロックしていないか確認（ブラウザがバックグラウンドに回るとハートビートが止まることがあります） |
 
## セキュリティに関する注意
 
このアプリは手軽さを優先し、APIキー等を使わず `databaseURL` のみで動作する構成にしています。上記のルール例は「ルーム名を知っていれば誰でも読み書き可能」になるため、以下のような対策を検討してください。（セキュリティはがばがばですね）
 
- ルーム名を推測されにくい文字列にする（例：`TEDX2026`のような分かりやすい名前は当日限定にする）
- イベント終了後、Firebaseコンソールからルームのデータを削除する、またはルールを一時的に閉じる
- 本格的に複数イベントで使い回す場合は、Firebase Authenticationの導入や、ルールで書き込み内容のバリデーションを追加することを推奨します
## ファイル構成
 
- `kanpe.html` … このアプリ本体（HTML/CSS/JSすべて1ファイルに内包、Firebase SDKはCDN読み込み）
